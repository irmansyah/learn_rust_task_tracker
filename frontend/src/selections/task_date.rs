// use inquire::DateSelect;

// pub fn get_input_selection_date(prompt: &str) -> Result<String, Box<dyn std::error::Error>> {
//     let _date = DateSelect::new(prompt).prompt()
//         .map(|date| date.format("dd-mm-yyyy").to_string())?;

//     println!();
//     Ok(_date)
// }
